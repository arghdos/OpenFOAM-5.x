#ifndef STOPWATCH_H
#define STOPWATCH_H

#include "string.H"
#include "clock.H"

namespace Foam
{
    class StopWatch
    {
        private:
            string _name;
            clock timer;
            double totalTime;
        public:
            StopWatch()
            {
                this->totalTime = 0;
                this->_name = "autogenerated";
            }
            StopWatch(string myname)
            {
                this->totalTime = 0;
                this->_name = myname;
            }
            void start()
            {
                // init new clock
                this->timer = clock();
            }
            void stop()
            {
                this->totalTime += this->timer.elapsedClockTime();
            }
            double getTotalTime() const
            {
                return this->totalTime;
            }
            string name() const
            {
                return this->_name;
            }
    };
}

#endif
